spring:
  application:
    name: apigateway
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:localhost}:${NACOS_PORT:8848}
        username: nacos
        password: nacos
        namespace: local
      config:
        server-addr: ${NACOS_HOST:localhost}:${NACOS_PORT:8848}
        username: nacos
        password: nacos
        namespace: local
        group: DEFAULT_GROUP
        file-extension: yaml
        prefix: apigateway
        refresh-enabled: true

